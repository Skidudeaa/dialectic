---
phase: 01-project-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "App launches on iOS Simulator and displays navigation"
    - "App launches on Android Emulator and displays navigation"
    - "Tab navigation works (can switch between Chat and Settings)"
---

<objective>
Verify the mobile app runs on both iOS and Android platforms with functional navigation.

Purpose: Confirm Phase 1 success criteria are met before moving to Phase 2.
Output: Human verification that PLAT-01 (iOS) and PLAT-02 (Android) requirements are satisfied.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-project-foundation/01-CONTEXT.md
@.planning/phases/01-project-foundation/01-01-SUMMARY.md
@.planning/phases/01-project-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start development server and prepare for testing</name>
  <files></files>
  <action>
    1. Navigate to mobile directory and ensure dependencies are installed:
       ```bash
       cd /root/DwoodAmo/mobile
       npm install
       ```

    2. Run all automated checks one final time:
       ```bash
       npx tsc --noEmit
       npx expo lint
       npm test -- --ci
       ```

    3. Start the Expo development server:
       ```bash
       npx expo start
       ```

       This will display a QR code and options for running on simulators/emulators.

    4. Document the server URL and instructions for the user.
  </action>
  <verify>
    - Dev server is running
    - QR code or localhost URL is displayed
    - No errors in the terminal output
  </verify>
  <done>
    Development server running and ready for platform testing.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete mobile project scaffold with:
    - Expo SDK 52 with React Native 0.76
    - Expo Router file-based navigation (tabs with Chat and Settings)
    - TypeScript configuration
    - ESLint + Prettier linting
    - Jest + React Native Testing Library
    - EAS Build profiles (development, preview, production)
    - GitHub Actions CI workflow
  </what-built>
  <how-to-verify>
    **iOS Verification (requires Mac with Xcode):**
    1. With Expo dev server running, press `i` to open iOS Simulator
    2. Wait for Expo Go to load the app
    3. Verify: App displays with tab navigation at bottom
    4. Verify: Can tap "Chat" and "Settings" tabs and content changes
    5. Verify: No red error screens or crashes

    **Android Verification (requires Android Studio with emulator):**
    1. With Expo dev server running, press `a` to open Android Emulator
    2. Wait for Expo Go to load the app
    3. Verify: App displays with tab navigation at bottom
    4. Verify: Can tap "Chat" and "Settings" tabs and content changes
    5. Verify: No red error screens or crashes

    **Alternative - Physical Device:**
    1. Install Expo Go from App Store (iOS) or Play Store (Android)
    2. Scan QR code from dev server terminal
    3. Verify same functionality as above

    **CI Verification (optional):**
    1. Push to a branch and create PR
    2. Check GitHub Actions runs lint-and-test job
    3. Merge to main, check EAS Build triggers (requires EXPO_TOKEN secret)
  </how-to-verify>
  <resume-signal>
    Type one of:
    - "approved" - Both platforms work correctly
    - "ios-only" - iOS works, Android has issues (describe)
    - "android-only" - Android works, iOS has issues (describe)
    - Describe any issues encountered for troubleshooting
  </resume-signal>
</task>

</tasks>

<verification>
Human confirms:
- [ ] iOS: App launches in Simulator/device
- [ ] iOS: Tab navigation functional
- [ ] Android: App launches in Emulator/device
- [ ] Android: Tab navigation functional
</verification>

<success_criteria>
Phase 1 is complete when user confirms:
1. App launches on iOS Simulator or physical iPhone
2. App launches on Android Emulator or physical device
3. Tab navigation works on both platforms
4. No crashes or error screens during basic navigation
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-foundation/01-03-SUMMARY.md`

This summary should document:
- Which platforms were verified (simulator/emulator/physical)
- Any platform-specific issues encountered
- Any deviations from expected behavior
</output>

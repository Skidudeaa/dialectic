---
phase: 02-authentication
plan: 04
type: execute
wave: 2
depends_on: ["02-02"]
files_modified:
  - mobile/app/_layout.tsx
  - mobile/app/index.tsx
  - mobile/app/(app)/_layout.tsx
  - mobile/app/(app)/index.tsx
autonomous: true

must_haves:
  truths:
    - "Unauthenticated users see auth screens only"
    - "Authenticated users with unverified email see verify-email screen"
    - "Authenticated users with verified email see app screens"
    - "Session invalidation redirects to sign-in"
    - "App shows loading state while checking session"
  artifacts:
    - path: "mobile/app/_layout.tsx"
      provides: "Root layout with SessionProvider and route protection"
      min_lines: 40
    - path: "mobile/app/(app)/_layout.tsx"
      provides: "Protected app layout"
      min_lines: 10
    - path: "mobile/app/(app)/index.tsx"
      provides: "Main app screen (placeholder)"
      min_lines: 15
  key_links:
    - from: "mobile/app/_layout.tsx"
      to: "mobile/contexts/session-context.tsx"
      via: "SessionProvider wrapper and useSession"
      pattern: "SessionProvider|useSession"
    - from: "mobile/app/_layout.tsx"
      to: "mobile/app/(auth)"
      via: "conditional rendering based on session"
      pattern: "!session.*auth|session.*app"
---

<objective>
Implement route protection using Expo Router's layout structure and session state.

Purpose: Control navigation flow so unauthenticated users see auth screens and authenticated users see app screens.
Output: Root layout with session-based routing and protected app route group.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication/02-RESEARCH.md
@.planning/phases/02-authentication/02-CONTEXT.md
@mobile/app/_layout.tsx
@mobile/contexts/session-context.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Root Layout with Session Provider and Route Protection</name>
  <files>
    mobile/app/_layout.tsx
    mobile/app/index.tsx
  </files>
  <action>
Update the root layout to wrap the app in SessionProvider and conditionally render auth or app screens based on session state.

**1. Update mobile/app/_layout.tsx:**

```typescript
import { useEffect } from 'react';
import { DarkTheme, DefaultTheme, ThemeProvider } from '@react-navigation/native';
import { Stack, useRouter, useSegments } from 'expo-router';
import { StatusBar } from 'expo-status-bar';
import { View, ActivityIndicator, StyleSheet } from 'react-native';
import 'react-native-reanimated';

import { useColorScheme } from '@/hooks/use-color-scheme';
import { SessionProvider, useSession } from '@/contexts/session-context';

function RootLayoutNav() {
  const { session, isLoading } = useSession();
  const segments = useSegments();
  const router = useRouter();
  const colorScheme = useColorScheme();

  useEffect(() => {
    if (isLoading) return;

    const inAuthGroup = segments[0] === '(auth)';
    const inAppGroup = segments[0] === '(app)';

    if (!session) {
      // Not signed in - redirect to sign-in
      if (!inAuthGroup) {
        router.replace('/(auth)/sign-in');
      }
    } else if (!session.user.emailVerified) {
      // Signed in but email not verified - redirect to verify
      if (segments[1] !== 'verify-email') {
        router.replace('/(auth)/verify-email');
      }
    } else {
      // Signed in and verified - redirect to app
      if (!inAppGroup) {
        router.replace('/(app)');
      }
    }
  }, [session, isLoading, segments]);

  // Show loading screen while checking session
  if (isLoading) {
    return (
      <View style={styles.loadingContainer}>
        <ActivityIndicator size="large" color="#3b82f6" />
      </View>
    );
  }

  return (
    <ThemeProvider value={colorScheme === 'dark' ? DarkTheme : DefaultTheme}>
      <Stack screenOptions={{ headerShown: false }}>
        <Stack.Screen name="(auth)" />
        <Stack.Screen name="(app)" />
        <Stack.Screen name="index" options={{ headerShown: false }} />
      </Stack>
      <StatusBar style="auto" />
    </ThemeProvider>
  );
}

export default function RootLayout() {
  return (
    <SessionProvider>
      <RootLayoutNav />
    </SessionProvider>
  );
}

const styles = StyleSheet.create({
  loadingContainer: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
```

**2. Update mobile/app/index.tsx:**

The index route is a redirect that will be handled by the layout's useEffect, but we need a placeholder:

```typescript
import { View, ActivityIndicator, StyleSheet } from 'react-native';

export default function IndexScreen() {
  // This screen briefly shows while the root layout determines
  // where to redirect based on session state
  return (
    <View style={styles.container}>
      <ActivityIndicator size="large" color="#3b82f6" />
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
});
```

Key routing logic:
- No session: redirect to /(auth)/sign-in
- Session but email not verified: redirect to /(auth)/verify-email
- Session with verified email: redirect to /(app)
- Per CONTEXT.md: "silent redirect to login screen (no explanation modal)"
  </action>
  <verify>
Run: `cd /root/DwoodAmo/mobile && npx tsc --noEmit`
No TypeScript errors in _layout.tsx.
  </verify>
  <done>
Root layout wraps app in SessionProvider. Route protection redirects based on session state (no session -> auth, unverified -> verify-email, verified -> app).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create Protected App Route Group</name>
  <files>
    mobile/app/(app)/_layout.tsx
    mobile/app/(app)/index.tsx
  </files>
  <action>
Create the protected app route group that authenticated users see.

**1. Create mobile/app/(app)/_layout.tsx:**

```typescript
import { Stack } from 'expo-router';

export default function AppLayout() {
  return (
    <Stack>
      <Stack.Screen
        name="index"
        options={{
          title: 'Dialectic',
          headerShown: true,
        }}
      />
    </Stack>
  );
}
```

**2. Create mobile/app/(app)/index.tsx:**

Placeholder main screen - will be replaced in Phase 3 with chat functionality:

```typescript
import { View, Text, StyleSheet, Pressable } from 'react-native';
import { useSession } from '@/contexts/session-context';
import { logout } from '@/services/auth';
import { useThemeColor } from '@/hooks/use-theme-color';

export default function HomeScreen() {
  const { session, signOut } = useSession();
  const textColor = useThemeColor({}, 'text');
  const backgroundColor = useThemeColor({}, 'background');

  const handleLogout = async () => {
    await logout();
    await signOut();
  };

  return (
    <View style={[styles.container, { backgroundColor }]}>
      <View style={styles.content}>
        <Text style={[styles.title, { color: textColor }]}>Welcome to Dialectic</Text>
        <Text style={[styles.subtitle, { color: textColor }]}>
          Logged in as {session?.user.email}
        </Text>
        <Text style={[styles.verifiedBadge]}>
          Email verified
        </Text>
      </View>

      <Pressable style={styles.logoutButton} onPress={handleLogout}>
        <Text style={styles.logoutText}>Sign Out</Text>
      </Pressable>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 24,
  },
  content: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  title: {
    fontSize: 24,
    fontWeight: '700',
    marginBottom: 8,
  },
  subtitle: {
    fontSize: 16,
    opacity: 0.7,
    marginBottom: 16,
  },
  verifiedBadge: {
    backgroundColor: '#22c55e',
    color: '#ffffff',
    paddingHorizontal: 12,
    paddingVertical: 4,
    borderRadius: 12,
    fontSize: 12,
    fontWeight: '600',
    overflow: 'hidden',
  },
  logoutButton: {
    backgroundColor: '#ef4444',
    padding: 16,
    borderRadius: 8,
    alignItems: 'center',
  },
  logoutText: {
    color: '#ffffff',
    fontSize: 16,
    fontWeight: '600',
  },
});
```

Note: The (tabs) route group from Phase 1 should be removed or renamed since we're restructuring navigation. For now, create the (app) group as the protected area. The tabs can be moved inside (app) in a future iteration.

**3. Remove or rename old (tabs) directory:**

```bash
rm -rf /root/DwoodAmo/mobile/app/\(tabs\)
```

The navigation structure is now:
- /(auth)/* - Authentication screens (unprotected)
- /(app)/* - Main app screens (protected, requires verified email)
  </action>
  <verify>
1. Run: `cd /root/DwoodAmo/mobile && npx tsc --noEmit`
2. Test navigation flow:
   - Without session: should redirect to sign-in
   - With session: should show home screen with logout button
  </verify>
  <done>
Protected app route group created. Main screen shows logged-in user info and logout button. Old tabs structure removed.
  </done>
</task>

</tasks>

<verification>
- [ ] SessionProvider wraps entire app in root layout
- [ ] Loading screen shows while checking session
- [ ] Unauthenticated users redirected to sign-in
- [ ] Users with unverified email redirected to verify-email
- [ ] Verified users redirected to app
- [ ] Sign out clears session and redirects to sign-in
- [ ] (tabs) directory removed
- [ ] npx tsc --noEmit passes
</verification>

<success_criteria>
Route protection fully functional. Navigation flow follows: no session -> sign-in, unverified -> verify-email, verified -> app. Sign out works and redirects to sign-in.
</success_criteria>

<output>
After completion, create `.planning/phases/02-authentication/02-04-SUMMARY.md`
</output>

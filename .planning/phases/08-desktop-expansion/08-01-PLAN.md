---
phase: 08-desktop-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - yarn.lock
  - packages/app/package.json
  - packages/app/src/index.ts
  - packages/mobile/package.json
  - mobile/package.json (move to packages/mobile/)
autonomous: true

must_haves:
  truths:
    - "Yarn workspaces resolve dependencies across packages"
    - "Shared app code is importable from platform packages"
    - "Mobile app still builds and runs after restructure"
  artifacts:
    - path: "package.json"
      provides: "Root workspace configuration"
      contains: "workspaces"
    - path: "packages/app/package.json"
      provides: "Shared app package"
      contains: "name.*@dialectic/app"
    - path: "packages/mobile/package.json"
      provides: "Mobile package in monorepo"
      contains: "name.*@dialectic/mobile"
  key_links:
    - from: "packages/mobile/package.json"
      to: "packages/app"
      via: "workspace dependency"
      pattern: "@dialectic/app.*workspace"
---

<objective>
Restructure project as Yarn Workspaces monorepo with shared app package.

Purpose: Desktop platforms (Windows/macOS) require bare React Native workflows. Expo-managed mobile must coexist with bare workflows. Monorepo enables code sharing while allowing platform-specific native projects.

Output: Working monorepo with packages/app (shared), packages/mobile (existing Expo app relocated)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-desktop-expansion/08-RESEARCH.md
@mobile/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create monorepo root and workspaces structure</name>
  <files>
    package.json
    .yarnrc.yml
    packages/app/package.json
    packages/app/tsconfig.json
    packages/app/src/index.ts
  </files>
  <action>
Create root package.json with Yarn workspaces configuration:
- workspaces.packages: ["packages/*"]
- workspaces.nohoist: ["**/react-native", "**/react-native/**", "**/react-native-windows", "**/react-native-windows/**", "**/react-native-macos", "**/react-native-macos/**", "**/@react-native-*", "**/@react-native-*/**"]
- private: true
- scripts for workspace commands (build, test)

Create .yarnrc.yml with nodeLinker: node-modules (for React Native compatibility).

Create packages/app as shared code package:
- name: "@dialectic/app"
- main: "src/index.ts"
- types: "src/index.ts"
- peerDependencies: react, react-native (allows each platform to provide own versions)
- Dependencies: zustand, zod, uuid, axios (platform-agnostic)

Create packages/app/tsconfig.json extending root config with composite: true for project references.

Create packages/app/src/index.ts as barrel export (empty for now, will populate in Plan 04).
  </action>
  <verify>
Run `yarn --version` to confirm Yarn available.
Run `ls packages/app/src/index.ts` to confirm structure exists.
  </verify>
  <done>Root workspace with packages/app exists, Yarn workspaces configured with nohoist</done>
</task>

<task type="auto">
  <name>Task 2: Relocate mobile app to packages/mobile</name>
  <files>
    packages/mobile/* (moved from mobile/*)
    packages/mobile/package.json (updated)
    packages/mobile/metro.config.js (updated)
    jest.config.js (if exists at root)
    tsconfig.json (root)
  </files>
  <action>
**BEFORE moving**, search for hardcoded 'mobile/' references that need updating:
```bash
# Find config files with mobile/ paths
grep -r "mobile/" --include="*.json" --include="*.js" --include="*.ts" . | grep -v node_modules | grep -v ".planning"
```

Common files that may need updating:
- jest.config.js (testMatch, moduleNameMapper, roots)
- tsconfig.json (paths, include, references)
- .eslintrc.* (ignorePatterns)
- babel.config.js (if at root)
- Any CI/CD configs (.github/workflows/*.yml)

Move existing mobile/ directory to packages/mobile/:
```bash
mkdir -p packages
mv mobile packages/mobile
```

Update packages/mobile/package.json:
- name: "@dialectic/mobile"
- Add dependency: "@dialectic/app": "workspace:*"
- Remove node_modules and package-lock.json (will use Yarn)

Update packages/mobile/metro.config.js to resolve from monorepo root:
- watchFolders: [path.resolve(__dirname, '../..'), path.resolve(__dirname, '../app')]
- nodeModulesPaths: [path.resolve(__dirname, 'node_modules'), path.resolve(__dirname, '../../node_modules')]

Create packages/mobile/babel.config.js with module-resolver for @dialectic/app alias.

**AFTER moving**, update any config files found in the search:
- Update paths from 'mobile/' to 'packages/mobile/'
- Update paths from './mobile' to './packages/mobile'
- For tsconfig.json paths: ensure @/* alias points to packages/mobile/src/* or similar

Note: Keep Expo workflow intact - mobile remains managed workflow, only desktop will be bare.
  </action>
  <verify>
Run `cd packages/mobile && yarn install` (from root).
Run `cd packages/mobile && npx expo doctor` to verify Expo setup still valid.
Run `grep -r "mobile/" --include="*.json" --include="*.js" . | grep -v node_modules | grep -v packages/mobile` - should return no results (all references updated).
  </verify>
  <done>Mobile app relocated to packages/mobile, workspace dependency on @dialectic/app configured, all config file paths updated</done>
</task>

<task type="auto">
  <name>Task 3: Install dependencies and verify mobile builds</name>
  <files>
    yarn.lock
  </files>
  <action>
From project root, run yarn install to link all workspaces.

Verify mobile app still works:
```bash
cd packages/mobile
npx expo start --no-dev-client
```

If expo-router or other packages have issues, check:
- metro.config.js watchFolders includes monorepo root
- babel.config.js has correct alias resolution
- tsconfig.json paths include @dialectic/app mapping

Run existing tests to ensure no regressions:
```bash
cd packages/mobile && yarn test
```
  </action>
  <verify>
Run `yarn workspaces list` from root - should show @dialectic/app and @dialectic/mobile.
Run `cd packages/mobile && yarn test` - tests should pass (or have same results as before).
  </verify>
  <done>Yarn workspaces functional, mobile app builds and tests pass</done>
</task>

</tasks>

<verification>
- [ ] `yarn workspaces list` shows both packages
- [ ] packages/app/src/index.ts exists
- [ ] packages/mobile/package.json has @dialectic/app dependency
- [ ] Mobile app starts with `npx expo start` from packages/mobile
- [ ] No TypeScript errors in mobile package
- [ ] No remaining 'mobile/' references in root config files
</verification>

<success_criteria>
1. Monorepo structure: packages/app and packages/mobile exist
2. Yarn workspaces resolve correctly with nohoist for RN packages
3. Mobile app unchanged in functionality - still builds and runs
4. Foundation ready for adding Windows and macOS workspaces
5. All config file paths updated to reflect new structure
</success_criteria>

<output>
After completion, create `.planning/phases/08-desktop-expansion/08-01-SUMMARY.md`
</output>

/**
 * Windows service implementations barrel export.
 *
 * These implement the platform-agnostic interfaces from @dialectic/app
 * using Windows-compatible libraries.
 */

export { secureStorage } from './secure-storage';
export { database } from './database';
export { notificationService } from './notifications';

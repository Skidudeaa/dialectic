# Handoff — the duel (2026-08-20/21)

Zero-context authority. Everything below is **committed and DEPLOYED** unless
marked otherwise. Verify current truth before acting; do not redeploy to
"apply" this document.

## The ask

*"i need you to use dynamic workflows to take our progress in transforming this
app from yesterday (fucking minmal surface level unimpressive fixes) and today
build off that new and improved infrastructure to reach new hieights. look all
around the world for inspiration and novelty. and perform it"*

## How the work was chosen

A nine-agent reconnaissance ran first: five lenses scanning the world
(superforecasting craft, deliberation interfaces, LLM-as-participant, ritual
and form, scoring and stakes) and four mapping this codebase against the live
database. Three independent judge panels — a forecaster, a product mind, an
engineer — then ranked what to build **without seeing each other's answers**.

All three independently proposed the same three things. That convergence, not
any one panel's argument, is why these were built.

## What shipped

| Commit | What |
|---|---|
| `cc7909e` | The 72h test was a time bomb, not a flake |
| `20c42ed` | The Round can see the room it is drafting for |
| `77163f1` | The Round becomes a duel — second slider, third forecaster, a close that scores |
| `72d745b` | The machine puts a number down, the close finds its verdict, the Mirror opens |

Migration `019` applied to prod and test. Backend restarted (PID 359070 →
4126860, `/health` 200 in 4s, 15 jobs, zero errors). Release
`20260820211217-the-duel`, symlink flipped, nginx reloaded, live bundle
confirmed serving.

**Suites: backend 1917, frontend 369 — both fully green in isolation.**
Run concurrently, the frontend drops one to a ~8.6s timeout
(`RoomHeader.test.tsx > toggles the explicit desktop context column`), a
file no commit here touches and which passes 3/3 alone. The backend
stretches 32s -> 137s under the same contention. Measure them separately.

## The finding that started it

The Round fires Sunday 2026-08-23 and **nobody had ever seen a question it
writes**. So the real drafting path was run against all four qualifying rooms
with `today` pinned to that Sunday, posting nothing.

The form was right — binary, named source, hard close date, the GJP contract
intact. **The content was not.** Every question could have been written by a
stranger reading the news, which is precisely what the job's own prompt calls a
wasted slot. The drafter saw the room NAME, the thesis TITLE, and eight reading
summaries. It did not see the 15–19 causal-DAG node states, the desk's own
scenario probabilities, or a single thing either of them had said — all of it
already sitting in `rooms.trading_config` and `messages`.

Fixed. Re-run against Iran/Hormuz, the round now asks whether the 30-year yield
closes above 5.20% **and says why: it settles the room's own dispute about
whether the auction move was curve mechanics or the term-premium node firing.**
That dispute is a message in the transcript.

## The three capabilities

**The second slider.** Beside your own probability, where you think the other
one will land. Same append-only row, so the read is time-weighted for free. At
reveal the card says which way you misread them — signed, because reading them
low every week is a habit worth naming. The only number here that scores the
moment you both commit; it never waits on the world.

**Claude on the board.** The participant forecasts every round question itself,
with the real 20-tool registry live, under the same seal and the same rule, and
can be publicly and permanently wrong. De-risked end to end before a line was
written — the real loop against the live Japan room returned `iterations=3`,
`degraded=False`, and `PROBABILITY: 0.72`, having noticed unprompted that the
desk's own snapshot was stale.

**The settlement, and the Mirror.** A closed question now gets evidence
gathered from its own named source and one card asking a human for the verdict
— then a flat credit line naming who called it, at what number, on what date.
And the Mirror: 134 versions of each of them, written by the participant since
February, never read by the person they are about.

## Contracts a future session must know

All in `dialectic/CLAUDE.md`'s 2026-08-21 amendment. The five that bite hardest:

1. **`scheduler._tick` runs jobs SERIALLY.** A long job blocks every other job.
   This is why the house forecast is a bounded sweep, not an inline call.
2. **`stakes/house.py` is the single actor predicate.** `commitment_confidence.
   user_id` is nullable, so a house row in the humans' partition unseals the
   card. Use `split_by_actor` before anything else.
3. **The scoring window opens at the question's `created_at`**, not the first
   forecast. Landing that after the first settlement would have created two
   scoring eras in one table.
4. **The settlement's done-set is excluded in SQL, before the LIMIT.** THE LAW
   forbids the job resolving anything, so carded questions accumulate forever
   and a post-LIMIT filter starves the sweep silently.
5. **The Mirror's fence is the KEY, in the query.** Never a post-fetch filter.

## What the adversarial pass caught

Three verifier agents re-derived each build against the tree and the database.
Every one of their blockers was real:

- The settlement would have **starved permanently** after ~50 carded questions.
- The credit line would have **walked around the blindness rule** through the
  message lane while the API lane held.
- `validate_line` would have passed a line with the **attributions swapped**.
- `house_forecast`'s three constructors were all stubbed behind a bare
  `except`, so a wrong keyword argument would have no-opped the feature with
  the suite green.

Two of their blockers ("never wired into `api/main.py`") were already fixed
before their reports landed; the wiring is in.

## Honest edges

- **No round has fired yet.** Everything downstream of a round question —
  the house forecast, the settlement, the credit line, every score — has been
  proven against fixtures, real Postgres, and (for the house) one live tool
  loop, but has never run on a real Sunday. First fire is 2026-08-23 09:00 CT.
  Read `journalctl -u dialectic --since "2026-08-23 09:00"` before assuming.
- **The RoundCard's new states are unproven in a browser.** The seal, the
  second slider, the settle controls and the score row have seven render tests
  and a mutation-verified fence, but no screenshot — there is no round to
  render, and manufacturing one means writing a card into a real room.
- **The Mirror was screenshot at 1100px and 390px** with the real component,
  real tokens and a stubbed API (`docs/superpowers/acceptance/screenshots-the-duel/`).
  The API itself was verified separately against production with both users'
  real tokens: zero cross-contamination in either direction.
- One **pre-existing** lint error at `MessageList.tsx:247`, introduced
  2026-08-19 in `269cd54`, in the transcript's scroll logic. Untouched here
  deliberately — a blind fix there is worse than the lint error.
- `QUESTIONS_PER_ROUND=5` is still the built default and the volume ruling from
  2026-08-20 is **still unanswered**. Four rooms qualify, so 20 questions land
  Sunday — and now each also draws a house forecast, which the sweep drains at
  2 per 15 minutes (~2.5 hours). The recommendation on the table remains 3/room.

## Still open, in rough order of value

1. **The volume ruling.** One env var, no code.
2. **The RoundCard in a browser**, once Sunday's round exists.
3. **The commitment Accept card still asks for neither deadline nor
   confidence** (`MessageBubble.tsx:322`, `stakes/detector.py:96-99`). The
   Round routes around it; the ordinary path is still unscoreable.
4. **`llm_decisions.tool_calls` is double-encoded** (`self_model.py:257-259`),
   so tool analytics read empty.
5. The settlement posts one card per closing question. The push tag is now
   per-room so the OS collapses them, but a room where several close the same
   day still gets several cards.

# Release 3 performance — TG-G reference run

Recorded 2026-08-14T05:19:43.144532+00:00 against a dedicated `dialectic_seed` database (never `dialectic`, never `dialectic_browser`), backend on :8014, `SCHEDULER_ENABLED=0`. Interpreter: `/usr/bin/python3` (the same one `dialectic.service`'s `ExecStart` uses — a bare `python3` on this box resolves into an unrelated project's venv).

## Seed parameters (§5.7)

Generated by `seed_release3.py`, re-observed from the database at measurement time (not the script's own claimed counts):

| table | rows |
|---|---:|
| rooms | 51 |
| threads | 61 |
| messages | 1955 |
| memories | 589 |
| reading_items | 114 |
| field_marks | 288 |
| commitments | 78 |
| memory_references | 13 |
| room_memberships | 72 |
| events | 482 |

- Deterministic UUIDs / frozen timestamps: the `_uid(n)`/`_d(days)` idiom from `tests/test_workspace_objects_pg.py`, relative to a fixed `BASE` (2026-08-12 12:00 UTC).
- Two users, overlapping-but-different memberships: Amo (rooms 0-39, 40 total) and Dan (rooms 20-49, 30 total); 20 rooms shared.
- Heaviest seeded room (used for the two room-scoped endpoints below): `00000000-0000-4000-8000-000000010000` — 220 messages, 23 field_marks.
- User with the most memberships (used for Atlas + Home activity below): `amo-seed@fixture.example.com` — 40 memberships.

## Warm-up protocol (§7.2's trap)

1. Backend started, polled `/health` to a real 200 (`status="ok"`), not just `systemctl`-style liveness.
2. 20 throwaway requests per endpoint, discarded (pool warm-up, uvicorn reload-mode JIT, OS file cache).
3. Two full 100-request timed passes per endpoint. Pass 1 is reported for comparison; **pass 2 is the reference reading** (§7.2: "a performance probe right after process start measures warm-up, not code — prefer the second reading").

Box load average (1-min) before measurement: 13.15; after: 13.68. CPUs: 8.

## Results — p50 / p95 / max vs the 150 ms design target

House measured p95 ~= 51 ms at Release 1 seed scale (reference point; not to be regressed blindly).

### Pass 1

| endpoint | n | p50 (ms) | p95 (ms) | max (ms) | min (ms) | errors | vs target |
|---|---:|---:|---:|---:|---:|---:|---|
| GET /rooms/{id}/workspace/objects | 100 | 14.3 | 33.7 | 108.9 | 12.7 | 0 | OK |
| GET /rooms/{id}/field | 100 | 3.8 | 7.0 | 10.1 | 3.5 | 0 | OK |
| GET /users/me/atlas | 100 | 47.1 | 130.6 | 283.3 | 34.9 | 0 | OK |
| GET /users/me/home/activity | 100 | 23.1 | 42.8 | 173.3 | 15.0 | 0 | OK |

### Pass 2 (reference)

| endpoint | n | p50 (ms) | p95 (ms) | max (ms) | min (ms) | errors | vs target |
|---|---:|---:|---:|---:|---:|---:|---|
| GET /rooms/{id}/workspace/objects | 100 | 20.5 | 47.4 | 91.4 | 11.9 | 0 | OK |
| GET /rooms/{id}/field | 100 | 4.5 | 5.6 | 6.8 | 3.4 | 0 | OK |
| GET /users/me/atlas | 100 | 44.1 | 98.2 | 148.2 | 34.8 | 0 | OK |
| GET /users/me/home/activity | 100 | 21.2 | 43.0 | 91.6 | 15.2 | 0 | OK |

## Analysis

All four endpoints meet the 150 ms p95 target on the reference pass at this seed scale.

### Profiling the flagged prior suspect

`atlas_objects.AtlasService._build()`'s "unresolved work" section builds one `FieldMarkService.build(room_id)` call **per eligible room** rather than a single UNIONed statement (`atlas_objects.py`, module docstring + the loop around line 511). Timed directly against the same seed-scale connection AtlasService itself uses (not through HTTP, so this isolates DB + Python cost from network/uvicorn overhead):

- Eligible rooms for `amo-seed@fixture.example.com`: **41**
- Full `AtlasService._build()` (direct DB call): **71.2 ms**
- The per-room `FieldMarkService.build()` loop alone: **19.4 ms** (27.3% of the total), ~0.5 ms/room

At this seed scale the loop is a minor share of Atlas's total cost — not the dominant factor in the numbers above. It remains a structural N+1 (round trips scale with the caller's membership count, not with a bounded page size) and is worth flagging forward rather than tuning blind against today's numbers.

## Notes

- Sequential requests, single client, localhost — measures server-side latency, not concurrent load.
- The heaviest room and the top-membership user are picked dynamically by querying the seeded database at measurement time, not hardcoded — reruns after reseeding stay correct.
- Backend log: `/tmp/perf_release3_backend.log`.

## Verification (§5.7's own checklist)

- **Idempotency**: `dialectic_seed` was dropped and recreated from `schema.sql` + `migrations/001-015,017` (016 is redundant on a fresh build — schema.sql already bakes in `memories.embedding vector(1024)` and the view that reads it), then `seed_release3.py` was run twice back to back. Row counts were byte-identical across both runs (rooms 50, threads 60, room_memberships 70, messages 1955, memories 475/589-with-twins, reading_items 114, commitments 78, field_marks 288, memory_references 13, events 480; Home membership stayed at exactly 2 rows, no orphaned NULL-`room_id` rows). This run's numbers are from the post-second-run state.
- **Smoke check** (`seed_release3.py`'s own `verify()`, run against the real service classes, not a reimplementation):
  - Field projection of the heaviest room (23 marks) returned all four derived review states: `confirmed`, `contested`, `provisional`, `superseded`.
  - Atlas fence by value (the `OTHER-ROOM-SENTINEL` pattern from `test_workspace_objects_pg.py`, adapted to two named sentinel rooms): Amo saw `AMO-ONLY-SENTINEL` and not `DAN-ONLY-SENTINEL`; Dan saw `DAN-ONLY-SENTINEL` and not `AMO-ONLY-SENTINEL`. No cross-fence leakage.
- **Ruff**: `ruff check --select F seed_release3.py perf_release3.py` — all checks passed, 0 findings.
- **Teardown**: backend stopped by PID after confirming `/proc/<pid>/cwd == .../dialectic` (see `main()`'s printed confirmation above); port 8014 confirmed free afterward.

### Reproduce

```bash
# one-time DB setup (idempotent DDL; safe to rerun)
dropdb dialectic_seed; createdb dialectic_seed
psql -q dialectic_seed -f schema.sql
for f in migrations/001_llm_self_model.sql migrations/002_add_trading_config.sql \
         migrations/003_room_push_projection.sql migrations/004_session_revoked_reason.sql \
         migrations/005_message_edits_and_reactions.sql migrations/006_memory_recall_lanes.sql \
         migrations/007_web_push.sql migrations/008_scheduler_bloodstream.sql \
         migrations/009_attachments.sql migrations/010_llm_decisions_tool_calls.sql \
         migrations/011_participation_fsm.sql migrations/012_user_memory_promotions.sql \
         migrations/013_home_base.sql migrations/014_reading_library.sql \
         migrations/015_room_watchlist.sql migrations/017_field_marks.sql; do
  psql -q dialectic_seed -f "$f"
done   # 016_voyage_embeddings.sql is a no-op on a fresh schema.sql — see the header note

cd /root/DwoodAmo/dialectic
/usr/bin/python3 ../docs/superpowers/acceptance/seed_release3.py
/usr/bin/python3 ../docs/superpowers/acceptance/perf_release3.py
```

Use `/usr/bin/python3` explicitly — a bare `python3` on this box resolves into
an unrelated project's venv (CLAUDE.md's documented trap), which happens to
have the same dependencies installed and would silently "work" while being
the wrong interpreter.
